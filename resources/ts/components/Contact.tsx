import React from 'react';

import { CONTACT_NAME } from '../constants/labels';

const Contact: React.FC = () => (
    <section id="contact">
        <h2>{CONTACT_NAME}</h2>
        <p>ここは連絡先のセクションです。</p>
    </section>
);

export default Contact;