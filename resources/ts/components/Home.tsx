import React from 'react';

import { HOME } from '../constants/labels';

const Home: React.FC = () => (
    <section id="home">
        <h2>{HOME}</h2>
        <p>ここはホームページのホームセクションです。</p>
    </section>
);

export default Home;