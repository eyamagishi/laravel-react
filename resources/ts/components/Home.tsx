import React from 'react';

import { HOME } from '../config/constants';

const Home: React.FC = () => (
    <section id="home">
        <h2>{HOME}</h2>
        <p>ここはホームページのホームセクションです。</p>
    </section>
);

export default Home;